Fonts are loaded via the Google Fonts CDN (see the @import at the top of
style.css) so the project stays dependency-free and works out of the box on
GitHub Pages. This folder is kept for anyone who wants to self-host the
Fraunces / Inter / Space Grotesk font files instead.
